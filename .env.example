# Copie para .env e preencha. NUNCA versione o .env real.
# Gerar chave forte: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY="troque_por_uma_chave_forte_aqui"

# Opcional. Default = backend/kinoplex.db (resolvido pelo main.py, independente do cwd).
# DATABASE_URL="sqlite:///caminho/absoluto/kinoplex.db"
